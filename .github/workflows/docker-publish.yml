name: Docker Publish

on:
  # Docs-only pushes are excluded by the upstream CI workflow, so they do not
  # produce the workflow_run completion that triggers this publish workflow.
  workflow_run:
    workflows:
      - CI
    types:
      - completed
    branches:
      - master

  workflow_dispatch:
    inputs:
      ref:
        description: Git ref, branch, tag, or commit SHA to publish
        required: true
        default: master

permissions:
  contents: read

concurrency:
  group: docker-publish-${{ github.event.workflow_run.head_sha || inputs.ref }}
  cancel-in-progress: false

jobs:
  docker:
    name: Build and push Docker image
    runs-on: ubuntu-latest

    if: >
      (github.event_name == 'workflow_run' &&
       github.event.workflow_run.conclusion == 'success') ||
      github.event_name == 'workflow_dispatch'

    steps:
      - name: Resolve source revision
        id: source
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_run" ]]; then
            echo "ref=${{ github.event.workflow_run.head_sha }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${{ inputs.ref }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout tested revision
        uses: actions/checkout@v7
        with:
          ref: ${{ steps.source.outputs.ref }}
          fetch-depth: 0

      - name: Resolve commit SHA
        id: commit
        shell: bash
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "short_sha=$(git rev-parse --short=7 HEAD)" >> "$GITHUB_OUTPUT"

      - name: Verify workflow revision
        if: github.event_name == 'workflow_run'
        shell: bash
        run: |
          test "${{ steps.commit.outputs.sha }}" = "${{ github.event.workflow_run.head_sha }}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract application Docker metadata
        id: app-meta
        uses: docker/metadata-action@v6
        with:
          images: rssmonster/rssmonster
          tags: |
            type=raw,value=latest,enable=${{ github.event_name == 'workflow_run' }}
            type=raw,value=sha-${{ steps.commit.outputs.short_sha }}
            type=ref,event=tag
          labels: |
            org.opencontainers.image.title=RSSMonster
            org.opencontainers.image.description=Self-hosted RSS aggregator and reader
            org.opencontainers.image.revision=${{ steps.commit.outputs.sha }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}

      - name: Extract inference Docker metadata
        id: inference-meta
        uses: docker/metadata-action@v6
        with:
          images: rssmonster/rssmonster-inference
          tags: |
            type=raw,value=latest,enable=${{ github.event_name == 'workflow_run' }}
            type=raw,value=sha-${{ steps.commit.outputs.short_sha }}
            type=ref,event=tag
          labels: |
            org.opencontainers.image.title=RSSMonster Inference
            org.opencontainers.image.description=Inference service for RSSMonster
            org.opencontainers.image.revision=${{ steps.commit.outputs.sha }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}

      - name: Build and push application
        id: app-build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          pull: true
          tags: ${{ steps.app-meta.outputs.tags }}
          labels: ${{ steps.app-meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      - name: Build and push inference
        id: inference-build
        uses: docker/build-push-action@v7
        with:
          context: ./inference
          file: ./inference/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          pull: true
          tags: ${{ steps.inference-meta.outputs.tags }}
          labels: ${{ steps.inference-meta.outputs.labels }}
          cache-from: type=gha,scope=inference
          cache-to: type=gha,mode=max,scope=inference
          provenance: mode=max
          sbom: true

      - name: Publish summary
        shell: bash
        run: |
          {
            echo "## Docker images published"
            echo
            echo "- Commit: \`${{ steps.commit.outputs.sha }}\`"
            echo "- Application digest: \`${{ steps.app-build.outputs.digest }}\`"
            echo "- Inference digest: \`${{ steps.inference-build.outputs.digest }}\`"
            echo "- Application tags:"
            echo '```'
            echo "${{ steps.app-meta.outputs.tags }}"
            echo '```'
            echo "- Inference tags:"
            echo '```'
            echo "${{ steps.inference-meta.outputs.tags }}"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
