# Production bundle-size budgets

Run `npm run verify:build` to create a production build and check its initial
entry assets. When `dist` already contains a current production build,
`npm run check:bundle-size` runs the check without rebuilding.

The checker reads Vite's generated `.vite/manifest.json`, so it identifies the
entry JavaScript and CSS plus the `icons-vendor`, `vue-vendor`,
`axios-vendor`, and `bootstrap-vendor` chunks without relying on content hashes.
Both raw and gzip bytes are checked.

Budgets live in `bundle-size-budgets.json`. Update them intentionally only
after reviewing the production diff and recording a new clean-build baseline:

1. Run `npm run build` and `npm run check:bundle-size`.
2. Confirm that any increase is expected and that lazy-loading boundaries have
   not moved into the initial entry accidentally.
3. Adjust only the affected raw and gzip limits, retaining a small amount of
   headroom above the measured size.
4. Include the old size, new size, and reason for the increase in the change
   description.

The budgets are regression guards, not long-term performance targets. Larger
icon-system or Bootstrap CSS reductions should be handled as separately
measured changes.

## Bootstrap class boundary guard

Run `npm run check:bootstrap-classes` to scan Vue and JavaScript source before
changing Bootstrap component imports or generated utilities. `npm run
verify:build` runs this fast source check before producing and measuring the
bundle.

The guard recognizes static `class` attributes, simple string values inside
`:class` bindings, and directly returned class strings. It intentionally does
not claim to resolve arbitrary runtime template strings. When a finite
Bootstrap class template is necessary, enumerate every audited outcome in
`DYNAMIC_CLASS_ESCAPES` in `scripts/check-bootstrap-classes.js`.

To extend the boundary safely, first enable the corresponding Bootstrap SCSS
module or utility-map family, then add that family to the matching explicit
allowlist and add or update a guard test. Do not add an allowlist entry only to
silence a failure.

`ENABLED_UTILITY_FAMILIES` mirrors the keys retained in the Sass `$utilities`
map. Responsive utility generation has its own allowlist and is currently empty
because the client uses no responsive utility tokens. Responsive grid classes,
including `col-md-*`, `offset-md-*`, and gutter classes such as `g-3`, are
generated by Bootstrap's grid module rather than `utilities/api`. Bootstrap
helpers remain enabled separately for `visually-hidden` and `text-bg-*`.

## Client production CSS baseline

This baseline was measured on 2026-07-30 from commit
`19543f9c78e71ea6dc144499ad8260132d775e22`. Client production sources matched
that commit; the unrelated uncommitted server change did not affect this build.
The build used Node.js 22.23.2, npm 10.9.8, and Vite 8.1.5 under WSL Ubuntu.

The bundle-size checker defines initial-entry CSS as the `css` array on the
manifest's `index.html` entry. That asset measured:

| Asset | Raw bytes | Gzip bytes |
| --- | ---: | ---: |
| `assets/index-D-bdvoHg.css` | 390,175 | 54,421 |

The entry also statically imports `icons-vendor`, whose stylesheet is linked in
the generated `index.html`. It is part of the initial network load but is not
included in the checker's `mainCss` measurement:

| Asset | Raw bytes | Gzip bytes |
| --- | ---: | ---: |
| `assets/icons-vendor-h3HWxWDu.css` | 2,284 | 527 |
| Initial-load CSS total (two responses) | 392,459 | 54,948 |

Gzip totals are the sum of independently compressed responses, matching how the
checker calls Node.js `gzipSync()` for each asset. No bundle-size budget was
changed when recording this baseline.

### Initial-entry CSS sources

The manifest assigns `assets/index-D-bdvoHg.css` to the `index.html` entry.
The statically reachable, style-bearing source files in that entry are:

- `src/main.js`, which imports `src/assets/styles/theme.css`.
- `src/App.vue` and `src/AppShell.vue`, which each import
  `src/assets/scss/global.scss`; that file imports Bootswatch Flatly variables,
  Bootstrap SCSS, and the Bootswatch Flatly theme.
- `src/App.vue`, `src/AppShell.vue`,
  `src/components/ActionErrorNotice.vue`, `src/components/Article.vue`,
  `src/components/ArticleEmptyState.vue`,
  `src/components/ArticleEndState.vue`,
  `src/components/ArticleListView.vue`,
  `src/components/ArticleLoadingState.vue`,
  `src/components/ArticleReaderLayout.vue`,
  `src/components/articles/ArticleContent.vue`,
  `src/components/articles/ArticleMedia.vue`,
  `src/components/BriefingContextText.vue`,
  `src/components/DailyBriefingIntro.vue`,
  `src/components/SmartFoldersGridOverview.vue`, and
  `src/components/UnreadSelectionContext.vue` contribute Vue `<style>` blocks.
- `src/main.js` imports
  `@dvuckovic/vue3-bootstrap-icons/dist/style.css`; the manifest assigns it to
  the statically imported `assets/icons-vendor-h3HWxWDu.css` asset.

### Lazy CSS sources

The following manifest records are dynamic entries. CSS remains lazy unless a
runtime path imports the corresponding entry. `AppError.vue` is also a dynamic
entry, but it has no manifest CSS asset.

| Manifest source | CSS asset | Raw bytes | Gzip bytes | Additional styled source files in the chunk |
| --- | --- | ---: | ---: | --- |
| `src/components/Sidebar.vue` | `assets/Sidebar-Cvrr1Scm.css` | 17,835 | 2,419 | `sidebar/SidebarActionButton.vue`, `SidebarCategoryGroup.vue`, `SidebarFeedItem.vue`, `SidebarNavItem.vue`, `SidebarSectionTitle.vue` |
| `src/components/DesktopToolbar.vue` | `assets/DesktopToolbar-BI6pmko3.css` | 28,568 | 4,306 | `model/Settings.vue`, `SettingsSectionError.vue`, `SettingsSectionLoading.vue`, `SettingsWelcome.vue` |
| `src/components/MobileToolbar.vue` | `assets/MobileToolbar-67sbBc-N.css` | 8,466 | 1,421 | — |
| `src/components/MobileMenuOverlay.vue` | `assets/MobileMenuOverlay-DCpAOsBM.css` | 10,036 | 1,819 | — |
| `src/components/ChatAssistant.vue` | `assets/ChatAssistant-DNS-xoSD.css` | 2,019 | 582 | — |
| `src/components/model/NewCategory.vue` | `assets/NewCategory-CbZzPsCH.css` | 1,277 | 424 | — |
| `src/components/model/NewFeed.vue` | `assets/NewFeed-BZx-sQpf.css` | 7,302 | 1,557 | — |
| `src/components/model/DeleteCategory.vue` | `assets/Cleanup-mWyGEolG.css` | 162 | 147 | Shared identical CSS asset |
| `src/components/model/DeleteFeed.vue` | `assets/Cleanup-mWyGEolG.css` | 162 | 147 | Shared identical CSS asset |
| `src/components/model/RenameCategory.vue` | `assets/RenameCategory-BquIuwh8.css` | 1,277 | 423 | — |
| `src/components/model/UpdateFeed.vue` | `assets/UpdateFeed-CcHRoEbP.css` | 379 | 227 | — |
| `src/components/model/Cleanup.vue` | `assets/Cleanup-mWyGEolG.css` | 162 | 147 | Shared identical CSS asset |
| `src/components/model/SettingsManageUsers.vue` | `assets/SettingsManageUsers-CuwCPAx8.css` | 7,473 | 1,473 | — |
| `src/components/model/BriefingPreferencesModal.vue` | `assets/BriefingPreferencesModal-Bhx_KIXu.css` | 11,275 | 2,131 | — |
| `src/components/model/UnreadConfigurationModal.vue` | `assets/UnreadConfigurationModal-BUgd4-_O.css` | 6,026 | 1,421 | — |
| `src/components/onboarding/InitialFeeds.vue` | `assets/InitialFeeds-BtLq_Do6.css` | 46 | 66 | — |
| `src/components/model/SettingsSmartFolders.vue` | `assets/SettingsSmartFolders-DWTb9Vlh.css` | 7,698 | 1,497 | — |
| `src/components/model/SettingsActions.vue` | `assets/SettingsActions-CJiIgCyY.css` | 9,017 | 1,618 | — |
| `src/components/model/SettingsScores.vue` | `assets/SettingsScores-CVUwkerc.css` | 5,271 | 1,196 | — |
| `src/components/model/SettingsIslands.vue` | `assets/SettingsIslands-Qh9sY1ue.css` | 5,401 | 1,212 | — |
| `src/components/model/SettingsTopics.vue` | `assets/SettingsTopics-D3vWB5GG.css` | 3,532 | 828 | — |
| `src/components/model/SettingsCrawlStatistics.vue` | `assets/SettingsCrawlStatistics-LsA6fBpV.css` | 3,830 | 905 | — |
| `src/components/model/SettingsFeedsOverview.vue` | `assets/SettingsFeedsOverview-CMru7n50.css` | 9,947 | 1,791 | — |
| `src/components/model/SettingsOfficialSources.vue` | `assets/SettingsOfficialSources-Cu7TjbAg.css` | 3,981 | 948 | — |

### Measurement procedure

1. Use the Node.js version required by `package.json`, then run `npm run build`
   from `client/`. This creates a clean Vite production output because the
   default `dist` directory is emptied before the build.
2. Run `npm run check:bundle-size`. Record the `Initial-entry CSS` result; the
   checker reads `dist/.vite/manifest.json` and compresses the emitted file with
   Node.js `gzipSync()`.
3. In `dist/.vite/manifest.json`, locate the record with
   `"src": "index.html"` and `"isEntry": true`. Its `css` array is the
   budgeted initial-entry CSS. Recursively inspect its `imports` for additional
   CSS in the initial graph; this identifies `icons-vendor`.
4. Follow `dynamicImports` from the entry and from other dynamic entries.
   Records with `"isDynamicEntry": true` and a `css` array identify lazy CSS and
   its owning source module. Content hashes may change between builds, so use
   manifest relationships rather than matching filenames.
5. For an exact raw measurement, read each emitted asset as bytes. For the
   comparable gzip measurement, use Node.js `gzipSync(contents).byteLength`.
   The style-bearing modules listed above were cross-checked against the module
   sources in an otherwise identical Vite sourcemap build outside `dist`.
